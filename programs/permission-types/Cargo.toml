[package]
name = "aeko-permission-types"
description = "AEKO Permission Layer shared types — clearance tiers, roles, access results, and envelope formats"
documentation = "https://github.com/MilliHub-dev/aeko-chain/tree/main/programs/permission-types"
version = { workspace = true }
authors = { workspace = true }
repository = { workspace = true }
homepage = { workspace = true }
license = { workspace = true }
edition = { workspace = true }

[dependencies]
borsh = { workspace = true }
aeko-sdk = { workspace = true }

[lib]
crate-type = ["lib"]
name = "aeko_permission_types"

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]
