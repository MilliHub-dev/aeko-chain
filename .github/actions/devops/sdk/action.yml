name: External SDK DevOps
description: Validate selected external SDKs and prove changed package versions are releasable.

inputs:
  js:
    description: Validate the JavaScript SDK.
    required: false
    default: "false"
  node:
    description: Validate the Node.js SDK.
    required: false
    default: "false"
  python:
    description: Validate the Python SDK.
    required: false
    default: "false"
  rust:
    description: Validate the high-level Rust SDK.
    required: false
    default: "false"
  release-js:
    description: JavaScript SDK package source changed and must use a new publishable version.
    required: false
    default: "false"
  release-node:
    description: Node SDK package source changed and must use a new publishable version.
    required: false
    default: "false"
  release-python:
    description: Python SDK package source changed and must use a new publishable version.
    required: false
    default: "false"
  release-rust:
    description: Rust SDK package source changed and must use a new publishable version.
    required: false
    default: "false"

runs:
  using: composite
  steps:
    - name: Validate selected non-Rust SDKs
      if: ${{ inputs.js == 'true' || inputs.node == 'true' || inputs.python == 'true' }}
      shell: bash
      env:
        SDK_JS: ${{ inputs.js }}
        SDK_NODE: ${{ inputs.node }}
        SDK_PYTHON: ${{ inputs.python }}
        RELEASE_JS: ${{ inputs.release-js }}
        RELEASE_NODE: ${{ inputs.release-node }}
        RELEASE_PYTHON: ${{ inputs.release-python }}
      run: bash "$GITHUB_ACTION_PATH/validate-non-rust.sh"

    # Existing formatting debt in an unchanged SDK must not make a core CI
    # refactor look like an SDK release. When Rust SDK source changes, however,
    # formatting and package verification are mandatory before publication.
    - name: Rust SDK format
      if: ${{ inputs.rust == 'true' && inputs.release-rust == 'true' }}
      shell: bash
      run: bash .github/actions/devops/check-rustfmt-changed.sh apps/sdk/rust-client

    - name: Diagnose Rust SDK lockfile and target check
      if: ${{ inputs.rust == 'true' }}
      shell: bash
      run: |
        set -euo pipefail

        set +e
        cargo check -p aeko-rust-sdk --all-targets
        check_status=$?
        set -e

        lock_changed=false
        if ! git diff --quiet -- Cargo.lock; then
          lock_changed=true
          echo '=== Cargo.lock diff after Cargo resolution ==='
          git diff -- Cargo.lock
          git restore --worktree -- Cargo.lock
        fi

        if [ "$check_status" -ne 0 ]; then
          exit "$check_status"
        fi

        if [ "$lock_changed" = "true" ]; then
          echo 'Cargo.lock is stale; the diff above is the exact resolver update required.' >&2
          exit 2
        fi

    - name: Rust SDK lint
      if: ${{ inputs.rust == 'true' }}
      shell: bash
      run: cargo clippy --locked -p aeko-rust-sdk --all-targets -- -D warnings

    - name: Rust SDK tests
      if: ${{ inputs.rust == 'true' }}
      shell: bash
      run: cargo test --locked -p aeko-rust-sdk

    - name: Rust SDK release build
      if: ${{ inputs.rust == 'true' }}
      shell: bash
      run: cargo build --locked --release -p aeko-rust-sdk

    - name: Rust SDK package dry-run
      if: ${{ inputs.rust == 'true' && inputs.release-rust == 'true' }}
      shell: bash
      run: cargo publish --locked -p aeko-rust-sdk --dry-run

    - name: Require a new Rust SDK version
      if: ${{ inputs.release-rust == 'true' }}
      shell: bash
      run: |
        set -euo pipefail
        version=$(cargo metadata --no-deps --format-version 1 | jq -r '.packages[] | select(.name == "aeko-rust-sdk") | .version')
        test -n "$version"
        status=$(curl -sS -o /dev/null -w '%{http_code}' "https://crates.io/api/v1/crates/aeko-rust-sdk/${version}")
        case "$status" in
          404) ;;
          200)
            echo "aeko-rust-sdk@${version} already exists on crates.io. Bump the package version before release." >&2
            exit 1
            ;;
          *)
            echo "Unable to prove crates.io version availability for aeko-rust-sdk@${version}; HTTP ${status}." >&2
            exit 1
            ;;
        esac
