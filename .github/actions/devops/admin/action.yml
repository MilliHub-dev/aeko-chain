name: Operations Web DevOps
description: Fully validate, build, and optionally publish the AEKO Operations Web image.

inputs:
  validate-source:
    description: Run source lint, type-check, tests, and host build gates.
    required: false
    default: "true"
  build-image:
    description: Build the Operations Web container image.
    required: false
    default: "false"
  publish:
    description: Push the immutable SHA-tagged Operations Web image to Docker Hub.
    required: false
    default: "false"
  registry-user:
    description: Docker Hub namespace used when publishing.
    required: false
    default: ""
  sha-tag:
    description: Immutable release tag.
    required: true

runs:
  using: composite
  steps:
    - name: Guard Operations Web image publication branch
      if: ${{ inputs.publish == 'true' }}
      shell: bash
      run: |
        set -euo pipefail
        if [ "$GITHUB_REF" != "refs/heads/main" ] || [ "$GITHUB_EVENT_NAME" = "pull_request" ]; then
          echo "Operations Web image publication is permitted only from main." >&2
          exit 1
        fi

    - name: Validate Operations Web source
      if: ${{ inputs.validate-source == 'true' }}
      shell: bash
      env:
        HOST_BUILD: ${{ inputs.build-image != 'true' && 'true' || 'false' }}
      run: bash "$GITHUB_ACTION_PATH/validate.sh"

    - name: Build Operations Web image
      if: ${{ inputs.build-image == 'true' }}
      shell: bash
      env:
        PUBLISH: ${{ inputs.publish }}
        REGISTRY_USER: ${{ inputs.registry-user }}
        SHA_TAG: ${{ inputs.sha-tag }}
      run: |
        set -euo pipefail
        tags=()
        output=(--load)

        if [ "$PUBLISH" = "true" ]; then
          : "${REGISTRY_USER:?registry-user is required when publishing}"
          tags+=(--tag "${REGISTRY_USER}/aeko-operations-web:${SHA_TAG}")
          output=(--push)
        else
          tags+=(--tag "aeko-ci/aeko-operations-web:${SHA_TAG}")
        fi

        docker buildx build \
          --file docker/Dockerfile \
          --target operations-web \
          --cache-from "type=gha,scope=aeko-operations-web" \
          --cache-to "type=gha,scope=aeko-operations-web,mode=max,ignore-error=true" \
          "${tags[@]}" \
          "${output[@]}" \
          .

    - name: Verify PR-built Operations Web image exists
      if: ${{ inputs.build-image == 'true' && inputs.publish != 'true' }}
      shell: bash
      env:
        SHA_TAG: ${{ inputs.sha-tag }}
      run: docker image inspect "aeko-ci/aeko-operations-web:${SHA_TAG}" >/dev/null
